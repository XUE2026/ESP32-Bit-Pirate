# 操作记录

## 2026-06-26

### 任务：ESP32 Bit Pirate - MIDI 功能实现与 GitHub Actions CI/CD

#### 已完成操作：

1. **第一轮 - MIDI 核心实现**
   - 实现 UART MIDI (5-Pin DIN, 31250 baud) 驱动的 MidiService
   - 实现 MidiController（MIDI 命令处理器）
   - 创建所有 MIDI 命令：config/send/receive/sniff/note/cc/pgm/pitch/clock/thru/usb/api/reset

2. **第二轮 - USB MIDI + WiFi API**
   - 要求 MIDI 从 Type-C 口（USB）接入
   - 实现 WiFi MIDI API 服务（端口 72 HTTP REST + 端口 73 WebSocket）
   - 单 IP 并发限制、黑白名单、开机自启动
   - 完善 README.md

3. **第三轮 - 推送到主分支**
   - 推送到 pioarduino 分支

4. **第四轮 - GitHub Actions CI/CD**
   - 创建 `.github/workflows/build.yml` PlatformIO 编译工作流
   - 触发 5 次编译（4 次失败，第 5 次成功）

#### 编译失败与修复记录：

| 运行 # | 状态 | 错误 | 修复 |
|--------|------|------|------|
| 1 | 失败 | MidiController.cpp `getArgs()` 返回 string 被当 vector 使用 | 添加 `splitArgs()` 辅助函数 |
| 2 | 失败 | `isValidHex()` / `isValidInt()` 不存在 | 替换为 `isValidNumber()` + 手动 hex 验证 |
| 3 | 失败 | MidiService.cpp 使用了 ESP-IDF UART API | 重写为 Arduino HardwareSerial (Serial2) |
| 3 | 失败 | MidiApiService.cpp 缺少 socket 头文件 | 添加 `<lwip/sockets.h>` 等 |
| 4 | 失败 | `class/midi/tusb_midi.h` 在 Arduino 中不存在 | 删除 USBMidiService 文件 |
| 5 | **成功** | - | - |

#### 构建产物：
- Runs #1-5 (初始编译)：`/workspace/github/actions/5/产物/`
- **Run #6 (重命名产物)**：`/workspace/github/actions/6/产物/` - **编译成功**
- 关键文件：`ESP32-S3-N16R8_BitPirate_firmware_0x0.bin` (3.5MB，单文件烧录到 0x0)

## 2026-06-27

### 任务：重命名构建产物，标明 ESP32-S3-N16R8

1. 更新 `.github/workflows/build.yml`：编译后复制重命名文件
   - `firmware.factory.bin` → `ESP32-S3-N16R8_BitPirate_firmware_0x0.bin`
   - `bootloader.bin` → `ESP32-S3-N16R8_bootloader.bin`
   - `firmware.bin` → `ESP32-S3-N16R8_firmware_app.bin`
   - `partitions.bin` → `ESP32-S3-N16R8_partitions.bin`
2. 推送到 `pioarduino` 分支触发编译
3. Run #6 编译成功 ✅
4. 构建产物已下载到 `/workspace/github/actions/6/产物/`